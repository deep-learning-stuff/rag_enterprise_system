# Copia este fichero a `.env` si quieres cambiar credenciales o puertos.
# Docker Compose lee `.env` automáticamente. Sin `.env`, se usan estos mismos valores por defecto.

# Postgres
POSTGRES_USER=rag
POSTGRES_PASSWORD=rag
POSTGRES_DB=rag
DB_PORT=5432

# Backend
BACKEND_PORT=8000

# Frontend
FRONTEND_PORT=5173
